Формат версий: `x.y.z-rN` (r1..r99, затем `x.y.(z+1)-r1`).
Сборка: `./build.sh` (автоинкремент через `version.txt`) или `./build.sh <версия>`.
Собирается в `build/luci-app-rrws_<версия>_all.ipk`, бэкапы — в `backup_<timestamp>/`.

## 0.2.1-r40
- Дефолты параметров скана приведены к рабочим значениям: Хостов 40→500,
  Потоков 3→50, Пробных хостов (Discovery) 2→5 (Таймаут уже был 3с).
  Обновлены scan.js (value + fallback `||`), luci.rrws (дефолты saveSettings/
  scanStart) и wscan.sh (HOSTS_MAX=500, DISCOVER_HOSTS=5).

## 0.2.1-r39
- Фикс спид-теста «Результатов нет»: после отката warp-in-warp (r38) парсер
  speedResult в ucode вернулся к 5-полю, а wsspeed.sh пишет 6 (с пингом) —
  строки не распарсились. speedResult снова парсит `ep node loc rtt dl ul`,
  UI показывает пинг.

## 0.2.1-r38
- Откат warp-in-warp из RRWS: убраны секция UI, методы внутреннего аккаунта
  (registerInner/innerAccountStatus/renewInnerAccount/deleteInnerAccount),
  поле outer_endpoint и флаги -O/-K/-k в wscan.sh. Причина: warp-in-warp на
  ядерном kmod меняет только узел, но НЕ страну выхода (всегда RU), в отличие
  от userspace warpscout (DE). Полноценный warp-in-warp будет в новой версии
  RRWS2 на Go. Пинг в спид-тесте (r37) сохранён. Откат вернул wscan.sh/
  luci.rrws/scan.js к состоянию v0.2.1-r33.

## 0.2.1-r37
- Фикс: `outer_endpoint` добавлен в декларацию args scanStart (без этого
  ubus падал с «Invalid argument» при передаче поля). Проверено: scanStart с
  outer_endpoint поднимает внешний туннель и все эндпоинты выходят через его
  узел (напр. внешний 188.114.96.191:4500 → все FRA).

## 0.2.1-r36
- Warp-in-warp в UI/бэкенде: секция «Warp-in-warp (скан через внешний
  туннель)» с предупреждением про не-DME эндпоинт, полем внешнего эндпоинта
  (сохраняется в settings, `outer_endpoint`), кнопкой регистрации внутреннего
  аккаунта (идентична первому, `registerInner`/`innerAccountStatus`/
  `renewInnerAccount`/`deleteInnerAccount`, файл
  `/etc/rrws-inner-account.json`). scanStart передаёт `-O/-K/-k`; без
  внутреннего аккаунта возвращает ошибку. Спид-тест: добавлен пинг (ICMP RTT)
  к каждому эндпоинту в выводе и UI.

## 0.2.1-r35
- Бэкенд: методы внутреннего аккаунта для warp-in-warp —
  `innerAccountStatus` / `registerInner` / `renewInnerAccount` /
  `deleteInnerAccount`. Внутренний аккаунт хранится отдельно
  (`/etc/rrws-inner-account.json`), регистрация идентична первому
  (тот же wregister.sh, просто `-o` на другой файл).

## 0.2.1-r34
- Warp-in-warp в сканере (скрипт): wscan.sh поддержал флаги `-O <outer_ep>`
  (включить вложение), `-K <outer_acct>` (аккаунт внешнего туннеля),
  `-k <inner_acct>` (аккаунт внутренних воркеров). Поднимает один внешний
  обфусц. туннель (таблица 199), все воркеры (второй аккаунт) маршрутизируются
  через него (`ip rule from <worker-ip> lookup 199`) — каждый эндпоинт выходит
  через узел внешнего. Воркеры при вложении ограничены 3 (Cloudflare
  rate-limit через один внешний, как у warpscout -jt 1). Fallback на обычный
  скан, если внешний туннель не поднялся. Проверено: внешний AMS → все
  результаты AMS.

## 0.2.1-r33
- UI: галка «Исключить узел DME» теперь стабильно стоит ПОД dropdown
  исключения подсетей (раньше из-за асинхронной отрисовки dropdown могла
  оказаться над ним вплотную) с увеличенным вертикальным отступом.

## 0.2.1-r32
- UI: подпись чекбокса «Исключить узел DME (Москва)» без лишнего
  «при выборе лучшего» (исключение теперь глобальное); увеличен отступ
  между блоком «Исключить подсети» и галкой DME.

## 0.2.1-r31
- Стилевая правка: узел/страна в спид-тесте теперь отображаются плоским
  текстом (`text-muted`), как в результатах скана, а не бейджем `label-info`
  с подложкой — единообразно во всём UI.

## 0.2.1-r30
- Спид-тест: (1) в выводе и UI показан узел/страна каждого тестируемого
  эндпоинта (например WAW RU); (2) прогресс-бар считает долю текущего
  эндпоинта (index/total), а не завершённых (index-1)/total — «6/10» теперь
  даёт 60%, а не 50%. README: добавлен раздел «Узел (NODE) и страна (COUNTRY)»
  — объяснение, почему можно подключиться к Варшаве (WAW), а сайты всё равно
  видят РФ (узел входа ≠ страна выхода; DME-исключение влияет на DPI-фильтрацию,
  а не на видимую страну).

## 0.2.1-r29
- Фикс спид-теста «0 кбит/с»: `speed.cloudflare.com` через WARP-туннель
  резолвится в FakeIP (198.18.x), и curl к нему даёт http 000 (скорость 0),
  хотя данные через туннель идут (trace работает). Теперь wsspeed.sh резолвит
  реальный anycast IPv4 через внешний DNS (1.1.1.1), с fallback на известный
  рабочий IP (104.18.7.198), и передаёт `--resolve` в curl. Проверено: dl
  через туннель с --resolve даёт реальную скорость.

## 0.2.1-r28
- Потолок воркеров снижен 70 → 50 (`JOBS_MAX`, `-gt 50`, UI 1-50). После r27
  воркеры обфусцированы и пересоздают интерфейс на каждый эндпоинт
  (CPU-bound): при 30-40 воркерах load 13-20 на 2-ядерном роутере, больше
  потоков не ускоряют скан, только грузят CPU. 50 — компромисс времени и
  нагрузки.

## 0.2.1-r27
- Воркеры скана теперь ОБФУСЦИРОВАНЫ (Jc/Jmin/Jmax+S1-S4+H1-H4+I1 через
  setconf, endpoint запечён в конфиг), как wsspeed/bootstrap. Раньше воркеры
  были необфусцированы (`amneziawg set`), DPI рвал их сразу после handshake
  (rx=0, данные не шли), поэтому trace_meta был пуст и узел «всегда DME» был
  артефактом. С обфускацией curl через воркер-туннель даёт реальные узлы
  (проверено: colo=WAW и др.). Смена эндпоинта — пересоздание интерфейса
  (kmod oops правило: нельзя мутировать живой обфусц. интерфейс).
  Discovery оставлен на необфусц. handshake-only (`try_hs`) — ему данные не
  нужны, только определить рабочие порты.

## 0.2.1-r26
- ФИКС корня: скан теперь читает реальный узел/страну выхода через КОНКРЕТНЫЙ
  эндпоинт. Раньше каждый воркер (wgscanN, allowed-ips 0.0.0.0/0) претендовал
  на main default-маршрут, ядро выбирало один из 70 туннелей, и
  `curl --interface <worker-ip>` для meta шёл в НЕПРАВИЛЬНЫЙ туннель -> узел
  всегда DME (Москва), хотя до зарубежных эндпоинтов (AMS/FRA/WAW) туннель
  доходил. Теперь каждому воркеру дана своя таблица маршрутизации
  (200+w) + `ip rule from <worker-ip>/32 lookup 200+w prio 98` (как в
  wsspeed/bootstrap), трафик meta гарантированно идёт через свой туннель.
  Это объясняет, почему warpscout находил AMS/FRA/WAW, а мы только DME.

## 0.2.1-r25
- «Исключить узел DME» теперь ДРОПИТ эндпоинты на исключённых узлах из
  результата полностью (не просто ранжирует ниже): они не появляются ни в
  таблице, ни как .conf. Смысл тот же (`exclude_nodes` → `-e DME`), но
  фильтр стал глобальным — на уровне итогового вывода, а не сортировки.

## 0.2.1-r24
- Опция «Исключить узел DME»: чекбокс в UI, сохраняется в settings
  (`exclude_nodes`), в скан передаётся флаг `-e DME`. wscan.sh при сортировке
  ставит эндпоинты на исключённых узлах ниже рабочих (после них, но перед
  torn), так что `applyBest`/`BEST` предпочитает не-DME (например ARN), когда
  такие есть. Цель: на сетях РФ, где DME фильтруется DPI, лучший выбирается
  из не-Московских узлов. Бэкенд: `valid_nodes()` (только [A-Z0-9]{3,4}).

## 0.2.1-r23
- Фикс: парсинг ответа API при fallback на `v0i1909051800`. Новый API-путь
  оборачивает ответ в `"result": {...}` (id/token/config внутри), а старый
  `v0a4005` отдаёт плоский JSON. Парсер теперь использует `(.result // .)` —
  работает с обоими форматами. Без фикса API2 давал 200, но «FATAL: no id».
  Проверено тестом: v0a4005 → 404 (подменённый путь) → retry v0i1909051800 →
  200, аккаунт сохранён (id/token/peer/address/warp_enabled). Также проверена
  QUIC I1-маска: handshake OK на первом эндпоинте (bootstrap с iCloud
  первым, QUIC1 — резерв).

## 0.2.1-r22
- Регистрация: запасные пути из warp-config-generator (llimonix).
  (1) Bootstrap-туннель перебирает I1-маски в порядке: iCloud-DNS (как было)
  → 2 QUIC-маски (TLS/ClientHello-вид, проходят DPI, который отпечатывает
  iCloud-первый пакет). Смена I1 = пересоздание интерфейса (по правилам
  kmod). (2) Fallback API-путь `v0i1909051800` — если POST /reg через
  `v0a4005` ответил HTTP-ошибкой (не 000), регистрация повторяется через
  новую версию API (включая PATCH enable warp).

## 0.2.1-r21
- Фикс прогресса discovery: `total` теперь считается по худшему случаю —
  полный перебор идёт по каждому порту НА КАЖДЫЙ пробный хост
  (`HOSTS + 3 + HOSTS*53`), а не один проход списка (53). Раньше при 5
  пробных хостах done доходил до ~274 при total=67 (UI показывал
  «проверяю порты 274/67»). Плюс кламп done на total.

## 0.2.1-r20
- Discovery-фаза больше не выглядит зависанием: wscan.sh пишет прогресс
  `discovery:<done>:<total>` в progress-файл на каждую проверку порта/хоста,
  backend scanStatus выводит из него процент (0-80%, монотонный), UI
  показывает «Discovery Mode: проверяю порты X/Y...». Причина — на сетях с
  блокировкой UDP discovery уходил в полный перебор 53 портов на 5 пробных
  хостах (до ~5.5 мин) при прогресс-баре, застывшем на 0%.

## 0.2.1-r19
- Юридическая атрибуция: в пакет добавлен MIT-текст warpscout
  (`/usr/share/licenses/luci-app-rrws/warpscout-LICENSE`) и README обновлён
  разделом «Атрибуция: warpscout (MIT)». warpscout (© 2026 Nikita S.)
  лицензирован под MIT, наш проект — его переработка; MIT требует включить
  копирайт-уведомление в копии/существенные части, это соблюдено.

## 0.2.1-r18
- Переименование в UI: пункт меню (`admin/services/rrws`) и заголовок страницы
  теперь «RR WARP Scanner» (было «RRWS» / «RRWS — RouteRich WARP Scanner»).

## 0.2.1-r17
- Фикс UI теста скорости: поле «Эндпоинтов (1-N)» больше не позволяет ввести
  число сверх фактического числа живых эндпоинтов из результата скана.
  Причина — `clampInput(speedCountInput, 1, 40)` клампил к фиксированным 40,
  игнорируя динамический cap из `refreshSpeedAvailable`; при вводе без потери
  фокуса значение уходило в speedStart как есть. Теперь `clampInput` принимает
  max как функцию (динамический потолок), `speedCountCap` синхронизируется с
  `refreshSpeedAvailable`, и кнопка «Запустить тест» дополнительно клампит
  значение в `[1, speedCountCap]`. Бэкенд уже резал по avail (luci.rrws:524) —
  это была только защита UI.

## 0.2.1-r16
- Потолок воркеров снижен 197 → 70: `JOBS_MAX` в ucode (клампы
  saveSettings/scanStart), `-gt 70` в wscan.sh, `max`/`clampInput`/подпись
  «Потоков (1-70)» в scan.js. Причина — опытным путём на части роутеров
  197 потоков полностью ломает веб-интерфейс; 70 — стабильный компромисс
  времени и нагрузки (полный скан 4318 хостов занимает заметно дольше,
  чем при 197, но не вешает LuCI).

## 0.2.1-r15
- Скрипты перенесены из `/usr/libexec/rrws/` в `/usr/bin/` (OpenWrt-конвенция:
  исполняемые скрипты лежат в `bin`, а не `libexec`; у zeroblock бинарь тоже в
  `/usr/bin`). Затронуто: `root/usr/bin/{wscan,wregister,wsspeed}.sh`, `BIN_DIR`
  в luci.rrws, chmod в build.sh, install в Makefile, .gitattributes, clean_test.sh,
  README/AGENTS. Сборка r15 (ipk+apk) проверена — скрипты в `usr/bin/`, пустой
  `usr/libexec` из пакета убран.

## 0.2.1-r14
- postinst ipk и post-install apk переведены с `rpcd restart` на `rpcd reload`.
  reload (SIGHUP→exec_self) пересканирует `/usr/share/rpcd/ucode/` и НЕ
  сбрасывает LuCI-сессии, restart же «выкидывал» из веб-интерфейса при
  установке. Проверено на живом роутере (rpcd 2025.09.01~bba95191-r2):
  сессия ubus переживает reload (HTTP /ubus с sid отвечает `result:[0,...]`
  до и после), новый ucode-файл подхватывается без restart.
- apk-сборка больше не теряет post-install: скрипт передаётся через
  `apk mkpkg --script "post-install:<path>"` (вне --files-дерева, чтобы не
  ставился как обычный файл). Раньше apk вообще не имел post-install.

## 0.2.1-r13
- Лог скана: чекбокс «автообновление лога» — пока включён (по умолчанию),
  лог фетчится каждые 3с и прокручивается к новым строкам; снятие галочки
  останавливает поллинг, чтобы можно было прочитать ранние строки без
  прыгающего скролла (в r11 это был чекбокс «пауза», r13 инвертировал логику:
  `logAutoRefresh` вместо `logAutoPaused`).

## 0.2.1-r12
- wscan.sh: воркеры (`worker $w &`) запускаются ТОЛЬКО если у них есть хосты
  (`[ -s /tmp/wscan/hosts.$w.txt ]`). При `jobs > hosts` round-robin оставлял
  хвостовые воркеры без файла — они спавнились впустую.
- ucode: хелпер `to_int()` против NaN — `int('')` в этой сборке ucode даёт
  NaN, а NaN сериализуется в невалидный JSON («nan»), ломая `JSON.parse` в
  браузере. Использован для `alive_count`/`scanned1`/`scanned2` в scanStatus.

## 0.2.1-r11
- scan.js: все rpc-вызовы страницы переведены на `opts.nobatch = true` —
  обход LuCI-батчинга запросов (`flushRequestQueue`). Когда несколько вызовов
  (scanStatus + scanLog + account + settings) летят одним POST, при ответе
  массивом любой сбой (таймаут на роутере под 100% CPU) режектит ВЕСЬ батч с
  «No related RPC reply» и прогресс умирает. Отдельные XHR делают страницу
  устойчивее — падение одного вызова не роняет остальные.
- Лог-панель: чекбокс паузы автопрокрутки (в r13 заменён на автообновление).

## 0.2.1-r10
- (В архиве собранных ipk этого пакета нет — между r9 и r11 в `build/` только
  r9 и r11. Если r10 собиралась, её артефакт не сохранился; изменения за
  r9→r11 учтены в записях r11.)

## 0.2.1-r9
- ucode scanStart: уникальный `scanid` (`/tmp/wscan/scanid`, `time()`) — теперь
  монотонный нижний порог процента scanStatus чтит только для того же скана,
  устаревшее значение от прошлого прогона (или поллинг старой страницы) не
  может запереть прогресс-бар на 99% во время старта нового скана.
- Сразу после scanStart чистятся счётчики прошлого прогона
  (`hosts.txt`, `scanned.cnt`, `scanned2.cnt`, `alive*.txt`) — раньше был
  window, где scanStatus видел старые счётчики при `progress == 'starting'` и
  ошибочно классифицировал состояние как phase2 с 99%.

## 0.2.1-r8
- Discovery-фаза вынесена в отдельное состояние: wscan.sh пишет `discovery`
  в progress-файл, ucode scanStatus отдаёт `phaseName == 'discovery'`,
  scan.js показывает «Discovery Mode: сканирую порты...».
- discover_ports(): быстрый проход по общим фолбэкам (1701/4500/500) на ВСЕХ
  probe-хостах по 2 попытки — типичный случай «2408 режется» решается за
  секунды, а не полным перебором списка портов на каждый хост (полный список
  с n=1 только если и фолбэки мертвы).

## 0.2.1-r7
- Исключение подсетей из скана: в UI добавлена секция «Исключить подсети» —
  чекбоксы по каждой из 17 подсетей WARP-пула, сгруппированные по первому
  октету (162.159.x, 188.114.x, 8.x). Отмеченные подсети не попадают в список
  хостов для скана. Реализация: wscan.sh получил флаг `-x 'subnet ...'`
  (фильтрует `SUBNETS` до генерации списка хостов), бэкенд — поле `exclude`
  в saveSettings/scanStart и валидацию `valid_subnets()` (только известные
  подсети, без дублей), getSettings отдаёт `subnets` для восстановления
  чекбоксов, scan.js — секцию UI с сохранением в настройках.
- Фикс: в этой сборке ucode глобальные `join()`/`map()` ведут себя
  нестабильно (возвращают null/undefined), что ломало запись exclude в
  файл настроек и сборку аргумента `-x`. Заменены на ручные циклы
  (`join_list()` в ucode). Также `-x` обёрнут в двойные кавычки — одинарные
  обрывали внешнюю обёртку `sh -c '...'`, из-за чего терялась вторая
  подсеть в аргументе запуска.

## 0.2.1-r6
- Монотонные фазы скана: раньше фаза в статус-баре «прыгала» phase2 → phase1.
  Причина: все воркеры пишут в ОБЩИЙ `/tmp/wscan/progress` и перетирают label
  друг друга (воркер, закончивший свою часть, писал phase2, а ещё сканирующие
  поверх — phase1). Решение: `scanStatus` в ucode считает фазу из
  агрегированных счётчиков — `scanned.cnt` (все просканированные хосты) и
  `scanned2.cnt` (обработанные в фазе 2); label файла игнорируется. Фаза
  монотонна: phase1, пока `scanned1 < total`, дальше phase2. Плюс сохранён
  `percent = max(percent, prev)` от гонки.

## 0.2.1-r5
- Лимит воркеров поднят 32 → 197: `JOBS_MAX` в бэкенде (ucode, оба клампа
  saveSettings/scanStart) и max/clampInput в scan.js, подпись «Потоков (1-197)».
  wscan.sh: `-gt 197`. Потолок — адресное пространство воркеров:
  `172.16.7.(2+w)`, `.200` занят интерфейсом спид-теста `wgspeed`. При
  4318 хостах подъём воркеров даёт линейное ускорение (фаза 1: 32 воркера
  ≈ 27 мин, 197 ≈ 4.5 мин в худшем случае). Проверено на роутере: держит
  нагрузку, память не растёт, CPU 100% — норма для I/O-bound воркеров.

## 0.2.1-r4
- Расширение области сканирования до всего пула: потолок числа хостов
  поднят 600 → 4318 (17 подсетей × 254 октета /24). Раньше максимум 600
  давал ~36 октетов на подсеть (~14% каждого /24), теперь можно покрыть
  весь WARP-пул целиком. Изменены: `HOSTS_MAX` в бэкенде (ucode, оба
  клампа saveSettings/scanStart) и max/clampInput в scan.js; подпись поля
  «Хостов» подсказывает значение всего пула. Генератор wscan.sh уже умел
  полный проход (`-f` + `-n 4318` → BATCH=254), менять его не пришлось.
  IPv6 (пул 2606:4700:d0/d1::/48 у warpscout) сознательно не добавляем —
  у большинства пользователей нет native IPv6, а через туннели WARP-вход
  недоступен.

## 0.2.1-r3
- Прогресс-бары больше не остаются на экране после завершения: бар скана
  скрывался при завершении/остановке, бар спид-теста — при ошибке запуска
  (раньше они застревали на 100%/0% после окончания работы).

## 0.2.1-r2
- Плашка статуса аккаунта: во время регистрации/перерегистрации бейдж
  «Зарегистрирован/Не зарегистрирован» становится жёлтой плашкой
  «Регистрация...», после завершения возвращается в нормальное состояние
  (успех/неудача определяются по accountStatus). Это закрывает пункт
  «в статус повесь плашку регистрации» из ревью (фон + маркер + поллинг
  были сделаны в r96, XHR-страховка — в r97).

## 0.2.1-r1
- Настройки/аккаунт/результат включаются в системный бэкап: добавлен
  `/lib/upgrade/keep.d/luci-app-rrws` (стандартный механизм OpenWrt,
  читается `/sbin/sysupgrade` наравне с `/etc/sysupgrade.conf`). В бэкап
  попадают `/etc/rrws-account.json`, `/etc/rrws-settings.json`,
  `/etc/rrws-last-result.txt` и восстанавливаются вместе с ним.
  Проверено на роутере: `sysupgrade -b` содержит все три файла.

## 0.2.0-r99
- «Скопировать .conf» после успешного копирования становится read-only
  (disabled, «Скопировано») на 2с — по фидбеку «скопировано лучше в RO кнопку»:
  не даёт нажать повторно и сразу видно, что копия сделана. При ошибке
  копирования кнопка остаётся активной для повторной попытки.

## 0.2.0-r98
- «Очистить» в блоке логов теперь действительно очищает логи, а не только поле
  на странице: новый rpcd-метод `scanLogClear` (ACL write) обнуляет
  `/tmp/rrws.rpc.log` и `/tmp/wscan.log` на роутере, поэтому после авто-обновления
  лог больше не возвращается.

## 0.2.0-r97
- XHR-страховка в стиле zeroblock: если запрос к роутеру реально не дошёл
  (rpcd перезапустился, сеть моргнула, или вызов упёрся в 20с rpc-таймаут),
  страница не виснет с «Ошибка: XHR request timed out», а сама перезагружается —
  состояние пересобирается с роутера (скан/спид-тест/регистрация крутятся в фоне
  и подхватываются). Все rpc-вызовы страницы идут через обёртку `rrwsDeclare`,
  глобальный `unhandledrejection` — страховка на необработанные промисы.
  Защита от цикла: не чаще одной перезагрузки в 30с (sessionStorage-маркер).

## 0.2.0-r96
- Исправлен «Ошибка регистрации: XHR request timed out». Причина: LuCI-клиент
  прерывает rpc-запросы через 20с (`L.env.rpctimeout`), а регистрация строила
  bootstrap-туннель и занимала 40-60с — браузер показывал ошибку, хотя аккаунт
  на роутере создавался.
- `register`/`renewAccount` теперь запускают wregister.sh в фоне (setsid) и
  возвращают `{started:true}` мгновенно; UI опрашивает `accountStatus` каждые
  2с до завершения (кнопки аккаунта и скана блокируются, показывается «Регистрация...
  N с»). Прогресс не теряется при F5 (подхват через `accountStatus.registering`).
- Маркер процесса: `/tmp/rrws-registering` (touch в wregister.sh + trap EXIT);
  лог регистрации: `/tmp/rrws-register.log` (новый метод `registerLog`, добавлен
  в ACL read). Успех/ошибка определяются по `registered` + смене `id` (для renew),
  при неудаче показывается хвост лога wregister.sh.

## 0.2.0-r95
- После F5/перезагрузки страницы запоминается состояние интерфейса:
  - результаты теста скорости снова показываются (берутся с роутера,
    `/tmp/wsspeed_result.txt`), без повторного запуска теста;
  - свёрнутость блока «Логи» сохраняется;
  - свёрнутость результатов сканирования (кнопка «Свернуть/Развернуть»)
    сохраняется.
  Хранение через localStorage (по ключам `rrws.*`), на роутере ничего не меняется.

## 0.2.0-r94
- Тест скорости: добавлена полоска прогресса в том же стиле, что у скана
  (синяя заливка + тёмная плашка с белыми процентами по центру). Процент
  считается от `index/total`; показывается во время теста, скрывается по
  завершении/остановке, подхватывается при перезагрузке страницы.

## 0.2.0-r93
- Результаты сканирования: в шапке к строке «Скан от <дата>» добавлено
  «Всего - N» (общее число найденных эндпоинтов, как в кнопке «Свернуть»).

## 0.2.0-r92
- Обновление страницы больше не «теряет» идущий скан или тест скорости.
  Скан/тест живут на роутере как отвязанные процессы и не прерываются
  перезагрузкой — раньше UI просто не знал о них. Теперь при загрузке
  страницы вызываются `scanStatus`/`speedStatus`, и если процесс ещё идёт,
  UI подхватывает его: показывает прогресс, кнопку «Остановить», блокирует
  кнопки и продолжает поллинг до завершения. Поллинг скана и теста вынесен
  в общие функции (`startScanPolling`/`startSpeedPolling`), используемые и
  при старте, и при подхвате.

## 0.2.0-r91
- Тест скорости: у каждой строки результата добавлена кнопка
  «Скопировать .conf» (прижата к правому краю через `margin-left:auto`,
  как в результатах скана). Конфиг собирается из тех же параметров
  (`confBase`), что и в скане.

## 0.2.0-r90
- Тест скорости: результаты сортируются по убыванию скорости скачивания (▼),
  при равенстве — по отдаче (▲). Раньше вывод шёл в порядке тестирования
  (порядок результата скана). Сортировка — в UI (`renderSpeed`), бэкенд
  отдаёт в исходном порядке.

## 0.2.0-r89
- Блокировки на время теста скорости: нельзя запустить скан и трогать
  аккаунт (регистрация/перерегистрация/удаление), пока идёт тест.
  - Бэкенд: `scanStart` возвращает ошибку при `speed_running()` (зеркально
    к уже имевшейся проверке в `speedStart`); `register`/`renewAccount`/
    `deleteAccount` — ошибку при `scan_running() || speed_running()`.
  - UI: на время теста отключаются кнопки «Найти туннели» и аккаунта;
    разблокируются после «Готово.» или «Остановить».

## 0.2.0-r88
- Кнопки «Быстрый поиск» и «Полный поиск (долго!)» заменены на одну
  «Найти туннели» с последовательным перебором. Режим `fast` был НЕ быстрее
  (время зависит только от числа хостов) и хуже по покрытию — он перебирал
  октеты вразброс (38, 75, 112...), а живые WARP-эндпоинты живут в низких
  октетах (`.1-.36`), которые покрывает последовательный проход. Бэкенд и
  `wscan.sh` сохранили поддержку `-f`/mode для совместимости.

## 0.2.0-r87
- Тест скорости: пояснение «эндпоинты на режущихся портах дадут 0» заменено
  на нейтральный дисклеймер «Тест носит справочный характер».

## 0.2.0-r86
- Прогресс-бар скана: проценты всегда видны и при 0% (раньше при нуле
  цифр не было). Чёрный текст с белой тенью тонул на светлой дорожке —
  заменён на тёмную плашку с белыми жирными цифрами по центру, читается и
  на заливке, и на фоне.

## 0.2.0-r85
- Прогресс-бар скана: проценты вынесены из заливки в отдельный оверлей —
  теперь они зафиксированы по центру полоски и выделены жирным (с белой
  тенью для читаемости и на заливке, и на фоне). Раньше текст ехал вместе
  с шириной заливки.

## 0.2.0-r84
- Тест скорости: поле количества эндпоинтов теперь ограничивается фактически
  найденным числом рабочих эндпоинтов ещё на вводе. Новый rpcd-метод
  `speedAvailable` (read) отдаёт `{available}`; UI при загрузке и после каждого
  скана ставит `max` поля и подпись «Эндпоинтов (1-N): » под найденное число
  (не больше 40) и ужимает введённое значение. Бэкенд-кламп в `speedStart`
  остаётся страховкой.

## 0.2.0-r83
- Тест скорости: максимум эндпоинтов поднят с 20 до 40 (UI, кламп бэкенда
  `min(count, 40)`, скрипт `-n` ≤ 40). Список теперь строится по всему
  результату скана, а не по первым 20 строкам (`awk` фильтрует torn по
  всему файлу).
- Валидация: `speedStart` считает рабочие (не-torn) эндпоинты в результате
  скана и клампит запрошенное количество до них — нельзя запросить 40, если
  найдено 5. При 0 рабочих — ошибка `no working endpoints in the scan result`.
  Ответ возвращает `available`; UI при превышении показывает
  «найдено рабочих: N, тестирую: M».

## 0.2.0-r82
- Переименование в UI: «Спид-тест туннелей» → «Тест скорости туннелей»,
  кнопка «Запустить спид-тест» → «Запустить тест скорости», статусы
  «Спид-тест...» → «Тест скорости...», «Нет результатов спид-теста» →
  «Нет результатов теста скорости». Внутренние ubus-методы (speedStart и т.п.)
  не менялись.

## 0.2.0-r81
- Спид-тест: поясняющий текст в UI переформулирован нейтрально. Утверждение
  «на этой сети handshake даёт только UDP 2408» было верно ТОЛЬКО для сети
  разработки (провайдер с DPI, проверено 2026-08-09) и вводило бы в
  заблуждение пользователей на других сетях, где 1701/4500/500 работают.
  Теперь: «эндпоинты на портах, которые провайдер режет, дадут 0».

## 0.2.0-r80
- Спид-тест: минимум времени замера поднят с 5 до 10с (UI, бэкенд-кламп
  `max(10,...)`) — на 5с при низкой скорости 25МБ даунлоад не успевал, цифры
  были ненадёжными. Поясняющий текст: «время на эндпоинт» — это только потолок
  curl, сверху добавляются подъём туннеля и ожидание handshake.
- `wsspeed.sh`: ожидание handshake (`HS_SWEEP`) теперь не превышает заданное
  время замера (`HS_SWEEP = min(8, MAX_TIME)`) — мёртвый эндпоинт не сжигает
  8с + время, а только бюджет времени. UI-минимум 10с ⇒ HS_SWEEP=8 не меняется.
- Пояснение в UI: на этой сети handshake через обфусцированный туннель даёт
  только UDP 2408 — эндпоинты на 4500/1701 дадут 0 (например «топовый»
  `162.159.193.1` вообще занят локальным интерфейсом роутера).

## 0.2.0-r79
- Фикс: upload в спид-тесте показывал 0 у ВСЕХ эндпоинтов, кроме первого.
  Причина — `teardown()` удалял `/tmp/wsspeed.upload.bin` после каждого
  эндпоинта, а следующий upload шёл с `--data-binary @/tmp/wsspeed.upload.bin`
  (файл уже отсутствовал → curl падал → ul=0). Уборка файла вынесена из
  teardown в финальную очистку после цикла. Проверено на роутере: 3 из 4
  эндпоинтов дают dl=75-107 Мбит/с и ul=81-85 Мбит/с.

## 0.2.0-r78
- Спид-тест найденных эндпоинтов: `wsspeed.sh` поднимает для каждого из
  топ-N рабочих эндпоинтов временный туннель (полный набор обфускации через
  `setconf` + policy routing table 102, как bootstrap в wregister.sh) и меряет
  download/upload через `speed.cloudflare.com/__down` и `/__up`.
- Бэкенд: методы `speedStart`/`speedStatus`/`speedResult`/`speedStop`/
  `speedLog` (прогресс `i/N ep`, результат `ip:port dl_kbps ul_kbps`).
- ACL: read `speedStatus`/`speedResult`/`speedLog`, write `speedStart`/
  `speedStop`.
- UI: секция «Спид-тест туннелей» с выбором количества эндпоинтов (1-20) и
  времени (5-30с), прогрессом и таблицей скоростей; кнопка «Запустить
  спид-тест» блокируется без аккаунта и во время скана.
- `speedStart` выбирает источник только если он непустой (иначе после
  пустого скана копировался пустой `wscan_result.txt` и спид-тест падал с
  «no scan result»).
- Проверено на роутере: ubus-путь целиком — 188.114.96.3:2408
  dl≈67.6 Мбит/с ul≈63.5 Мбит/с; 8.34.146.1:2408 dl≈75.3 Мбит/с.
- Примечание: на этой сети под DPI handshake даёт только обфусцированный
  интерфейс; обычный wscan (без обфускации) сейчас может находить 0 живых,
  хотя эндпоинты живы. Аккаунт был перерегистрирован (старый ключ handshake
  не давал) — новый валиден.

## 0.2.0-r77
- Фикс r76: после «Свернуть»→«Развернуть» кнопки «Скопировать .conf» /
  «Показать .conf» больше не уезжают влево. При разворачивании строкам
  возвращался `display=''`, что сбрасывало их inline `display:flex` на
  блочный — элементы переносились и кнопки падали вниз слева. Теперь
  возвращается явный `display:flex`.

## 0.2.0-r76
- В строках результатов кнопки «Скопировать .conf» / «Показать .conf»
  обёрнуты в группу с `margin-left:auto` — теперь они всегда прижаты к
  правому краю строки и не «уезжают» влево после «Свернуть»→«Развернуть»
  (раньше прижатие задавалось только через `flex:1` на адресе эндпоинта,
  и при переносе flex-wrap кнопки выпадали с выравниванием влево).

## 0.2.0-r75
- Кнопка «Свернуть» в блоке «Результаты сканирования»: сворачивает все
  строки, кроме лучшего (первого не-torn, помеченного BEST) эндпоинта,
  чтобы до блока «Логи» не нужно было долго скроллить. Появляется, когда
  результатов больше одного. В свёрнутом виде кнопка меняется на
  «Развернуть (N)» и возвращает полный список.

## 0.2.0-r74
- Лог ВО ВРЕМЯ операций с аккаунтом (Зарегистрировать WARP / Перерегистри-
  ровать / Удалить аккаунт) теперь обновляется сам каждые 3с и подтягивает
  финальное состояние по завершении — раньше авто-обновление лога работало
  только при сканировании, а при регистрации приходилось жать «Обновить лог».

## 0.2.0-r73
- Единое расстояние между всеми кнопками во всех блоках (скан, результат,
  логи, аккаунт): все группы переведены на flex с `gap:8px`. Раньше кнопки
  «Быстрый поиск»/«Полный поиск»/«Остановить» разделялись text-нодами и
  нативными margin'ами LuCI, из-за чего расстояние отличалось от блока
  «Скопировать лучший .conf»/«Показать .conf»/«Скачать всё .txt».

## 0.2.0-r72
- Пункт меню в службах теперь просто «RRWS» (убрано «— RouteRich WARP
  Scanner», слишком широко).
- Кнопка «Остановить» при сканировании больше не прилипает к соседней
  «Полный поиск (долго!)»: добавлен пробел и `margin-left: 1em`.

## 0.2.0-r71
- Полный рефакторинг имён: пакет `luci-app-warpscan` → `luci-app-rrws`
  (RouteRich WARP Scanner, RRWS). Переименованы все внутренние идентификаторы:
  ubus-объект `luci.warpscan` → `luci.rrws`, каталог `/usr/libexec/warpscan` →
  `/usr/libexec/rrws`, ucode-файл, ACL, пункт меню (`admin/services/warpscan`
  → `admin/services/rrws`, заголовок «RRWS — RouteRich WARP Scanner»),
  view-путь `warpscan/scan` → `rrws/scan`, файл аккаунта
  `/etc/rrws-account.json`, настройки `/etc/rrws-settings.json`,
  сохранённый результат `/etc/rrws-last-result.txt`, лог `/tmp/rrws.rpc.log`,
  скачиваемый архив `rrws-configs.txt`. Кнопка «Скачать всё» и binary name
  переименованы. Старые пути `/etc/warpscan-*.json` на роутере НЕ мигрируются
  автоматически — при обновлении с r70 аккаунт надо перерегистрировать
  (кнопка «Перерегистрировать»).

## 0.2.0-r70
- Блок «Логи» сворачивается/разворачивается кликом по заголовку (индикатор
  ▾/▸). Кнопки «Обновить лог» (зелёная, var(--success)) и «Очистить»
  (красная, var(--danger)) раскрашены цветами темы Routerich, текст+рамка
  без заливки фона кнопки.
- «Быстрый поиск» и «Полный поиск» заблокированы, пока аккаунт WARP не
  зарегистрирован (updateScanAuth, переприменяется после скана).

## 0.2.0-r69
- Проверка API через поднятый bootstrap-туннель: handshake на `wgregb` ещё
  не значит, что curl ходит в туннель (policy routing может не встать,
  эндпоинт занят локальным интерфейсом — см. r60). После `bootstrap_up`
  теперь probe GET $API/ через `--interface $BOOT_IP` с таймаутом 15с
  (probe() получил 3-й параметр timeout, по умолчанию 4с). Если туннель
  поднят, но API недостижим — bootstrap_down и фолбэк на opera (после r68).
- Проверено на живом роутере с nft-правилом `ip saddr 172.16.0.3 tcp dport
  443 drop` (UDP/2408 не тронут, bootstrap handshake OK, API-443 режется):
  bootstrap up → probe 000 → teardown → opera reuse → POST /reg=200,
  PATCH=200, аккаунт сохранён. Happy path (API достижим через туннель) тоже
  проверен: probe http 404 → регистрация через bootstrap-туннель.

## 0.2.0-r68
- Третий резервный путь к API в wregister.sh — Opera Proxy (`opera-proxy`,
  пакет Routerich): direct → existing tunnel → bootstrap → opera.
  При недоступности API напрямую/через существующий туннель и неудаче
  bootstrap-туннеля (все endpoint'ы отброшены handshake'ом) скрипт ставит
  opera-proxy (opkg install при отсутствии) и регистрируется через
  `curl -x http://127.0.0.1:18080`. Проверено на живом роутере под DPI:
  bootstrap fail (TEST-NET хосты) → opera POST /reg=200, PATCH=200, аккаунт
  сохранён, выход из exit-страны через Opera (RU).
- Умная работа с системным сервисом opera-proxy: если на `OPROXY_PORT`
  (18080) уже слушает экземпляр (пользовательский сервис с procd respawn) —
  он ПЕРЕИСПОЛЬЗУЕТСЯ и НЕ трогается на выходе; если порт свободен —
  поднимается собственный временный экземпляр, который гасится по PID
  (НЕ `killall`, чтобы не убить системный сервис). Teardown вынесен в
  `teardown_path()` (case по `IFACE_ARG`), описания путей — в `path_name()`.
- FATAL-ветки и teardown переведены с хардкода `--interface $BOOT_IP` на
  общие `teardown_path`/`path_name` — opera-путь теперь корректно чистится
  и в ветках ошибок, и при успехе.

## 0.2.0-r67
- Подробное логирование bootstrap-фазы в wregister.sh (видно в output
  ubus-метода register/renewAccount/scanStart auto-register и в
  /tmp/warpscan.rpc.log): каждый пробуемый endpoint + номер попытки,
  результат `setconf` (в т.ч. успех и stderr при ошибке), статус handshake
  на каждой итерации (1..3, возраст handshake), fallback на plain с rc,
  предупреждения при неудаче `ip route/rule add`, проверка маршрута
  (`ip route get`), итоговая сводка `tried N endpoints`.
- Убран `>/dev/null 2>&1` у вызова `bootstrap_up` — раньше логи фазы
  глушились и не попадали в лог; теперь видны все детали. Логика
  регистрации не менялась (только диагностика).

## 0.2.0-r66
- РЕАЛЬНОЕ решение DPI-блока регистрации, без отказа от обфускации.
  Причина зависаний r63/r64 найдена в kernel oops (pstore): kmod-amneziawg
  этого билда разыменовывает кривой указатель, когда junk-параметры заданы
  ЧАСТИЧНО (только jc/jmin/jmax/i1 без S1-S4/H1-H4) либо когда после
  `setconf` с обфускацией интерфейс мутируют в живую (`amneziawg set peer
  remove/add/endpoint`) — bad pointer → panic → reboot.
- Решение: bootstrap-интерфейс строится ТОЧНО как встроенные TestWarp/TW2
  этого роутера — полный набор обфускации (Jc=6 Jmin=10 Jmax=50 S1-S4=0
  H1-H4=1..4 I1=<DNS-masq>) через `amneziawg setconf <configfile>` с
  endpoint'ом ЗАПЕЧЁННЫМ в конфиг. После setconf `amneziawg set` на
  обфусцированном интерфейсе НЕ вызывается вовсе (см. comment в bootstrap_up);
  смена эндпоинта = пересоздание интерфейса целиком.
- Проверено на живом роутере под активным провайдерским DPI (direct 000):
  bootstrap handshake OK, POST /reg=200, PATCH=200, аккаунт сохранён,
  warp_enabled=true, интерфейс подчищен, uptime стабилен. Работает и через
  ubus (`luci.warpscan register`), пакет r66 установлен.

## 0.2.0-r65
- РЕШЕНИЕ DPI-блока регистрации: bootstrap-интерфейс теперь ставит ТОЛЬКО
  `i1` (маска handshake под DNS-запрос), БЕЗ `jc/jmin/jmax`. Провайдерский
  DPI убивает `api.cloudflareclient.com`, а через чистый туннель POST тоже
  таймаутил; с `i1` API достижим (POST /reg=200, PATCH=200), при этом
  junk-шторма в kmod нет — зависание из r63 не воспроизводится (проверено
  на живом роутере, uptime стабилен, интерфейс подчищен).
  Если kmod отвергает `i1` — bootstrap откатывается на чистый интерфейс.

## 0.2.0-r64
- ВАЖНО: обфускация (jc/jmin/jmax/i1) в bootstrap-туннеле УБРАНА обратно
  (r63 пробовал включить junk, как у warpscout). Проверка на роутере:
  при реальном трафике (POST /reg через туннель) kmod-amneziawg на этом
  ядре/ревизии ЗАВИС (роутер завис полностью, потребовался reboot;
  время 01:00-01:26 2026-08-09). Warpscout на ПК переживает DPI потому,
  что обфускация у него реализована в userspace (Go), а не в kmod.
  Bootstrap остаётся без обфуски (см. r60).
- r64 = стабильная регистрация без обфускации: bootstrap поднимается,
  но при активной DPI-блокировке api.cloudflareclient.com POST таймаутит
  (даже через туннель). Это сетевая особенность провайдера, не приложения.
- Синтаксис amneziawg на этом bilde: параметры junk задаются БЕЗ `=`
  (`jc 6 jmin 10 jmax 50`, `i1 '<str>'`) — это отличается от
  amneziawg-tools v1.0.20250903, где `jc=6` отвергалось.

## 0.2.0-r63
- Добавлена обфускация (jc 6 jmin 10 jmax 50 i1) в bootstrap-интерфейсе
  wregister.sh — как у warpscout iCloud I1. НЕ ПУБЛИКОВАТЬ: вызывает
  зависание kmod на реальном трафике (см. откат в r64).

## 0.2.0-r62
- wregister.sh: `warp_enabled` в /etc/warpscan-account.json теперь честно
  отражает реальное состояние после PATCH (`true` при http 200/204).
  Раньше поле копировалось из ответа POST /reg (всегда false, warp ещё
  выключен) и врало в файле даже после успешного enable.
- Подтверждено на чистом роутере (24.10.8, factory reset): установка ipk
  работает, kmod-amneziawg доустанавливается автоматически (r61),
  регистрация кнопкой в LuCI проходит напрямую (api доступна: http 404 на
  probe) — POST /reg 200, PATCH 200, аккаунт в /etc/warpscan-account.json.
  После opkg install LuCI/ssh «выкидывает» — это postinst делает
  `/etc/init.d/rpcd restart` (нужно, иначе ubus не видит ucode), рестарт
  сбрасывает сессии. Нормальное поведение, случается 1 раз при установке.

## 0.2.0-r61
- Зависимости пакета: добавлен `kmod-amneziawg` (в ipk Depends и apk depends).
  Раньше был только `amneziawg-tools` (userspace), который зависит лишь от
  `libc` и ядерный модуль НЕ тянет. На чистом роутере без kmod bootstrap и
  сам скан падали бы с «cannot create interface». Теперь opkg сам
  доустанавливает kmod-amneziawg (в RouteRich-фидах он есть).

## 0.2.0-r60
- Чиним bootstrap-туннель (r57-r59 давали ложный «успех»). Настоящая
  картина, выясненная на роутере с nft-блокировкой прямого пути:
  1. Ключи bootstrap работают, НО не на всех эндпоинтах: `162.159.193.1:2408`
     и `8.34.146.1:2408` handshake bootstrap-ключа НЕ дают (уже заняты
     локальными интерфейсами warp/TestWarp), а `188.114.96.3:2408`,
     `162.159.192.1:2408`, `188.114.97.1:2408` — дают. Порядок
     BOOT_HOSTS переставлен: сначала заведомо живые, занятые в конец.
  2. curl `--interface <ip>` сам по себе туннель не использует — нужен
     policy routing: `ip route add default dev wgregb table 101` +
     `ip rule add from 172.16.0.3/32 lookup 101 prio 98`. Только после
     этого трафик с 172.16.0.3 реально идёт через туннель (проверено:
     при заблокированном eth1 curl через туннель отдаёт trace, ping 0%
     loss). Остальной трафик роутера не затронут (rule точечный).
  3. cleanup: bootstrap_down снимает rule+route и удаляет интерфейс;
     при неудаче bootstrap_up тоже подчищает.
- Проверено: `-t nosuchiface` + nft drop на eth1 (api 000) → bootstrap
  поднялся, POST /reg http 200, PATCH http 200, аккаунт в файл, wgregb
  и rule/route удалены. Рабочий аккаунт /etc/warpscan-account.json и
  интерфейс warp не пострадали.

## 0.2.0-r59
- Исправлен ложный «no path to the API»: при прямом доступе IFACE_ARG
  пуст, а старый `[ -z "$IFACE_ARG" ]` трактовал это как отсутствие пути.
  Введён флаг PATH_OK. Также: любой HTTP-ответ (404 и т.п.) = API
  достижим (как warpscout apiReachable), блокировкой считается только
  curl_exit != 0 / http 000.

## 0.2.0-r57
- Регистрация аккаунта больше НЕ требует готового туннеля: если API
  api.cloudflareclient.com недоступен напрямую и существующий интерфейс
  (по умолчанию `warp`) тоже не работает, wregister.sh сам поднимает
  временный bootstrap-туннель на ключах warpscout
  (приватный `4OnO86dDLpqJ2U10ODwX3tarx6xlRGLfkmbSBtMgaHg=`,
  peer `bmXOC+F1FxEMF9dyiK2H5/1SUtzH0JuVo51h2wPfgyo=` — Cloudflare принимает
  handshake без зарегистрированного аккаунта). Bootstrap перебирает
  эндпоинты (первые живые + пулы warpscout) и порты 2408/1701/4500/500,
  через первый с успешным handshake делает POST /reg + PATCH, затем
  удаляет временный интерфейс. Теперь регистрация работает у всех, без
  ручного поднятого VPN.
- Bootstrap-интерфейс: `wgregb` (адрес 172.16.0.3). Ключ передаётся через
  временный файл /tmp, а не process substitution (надёжнее в busybox ash).
- Известная особенность: временный bootstrap-туннель строит ОДИН аккаунт
  с минимальной обфускацией (без Jc/Jmin/Jmax/H1-4/I1 на ядре — см. грабли
  про kmod-amneziawg). Если сеть режет все UDP-порты WARP, bootstrap не
  найдёт эндпоинт — тогда виден лог `bootstrap: no endpoint answered`.

## 0.2.0-r56
- Подробное логирование регистрации аккаунта:
  wregister.sh выводит в stderr диагностику каждого шага с префиксом
  `[wregister]` — генерация ключей, прямой probe API (curl_exit + http_code),
  fallback через туннель (проверка существования интерфейса), сам POST
  register (curl_exit + http_code + curl stderr + тело ответа при ошибке),
  PATCH enable warp, map curl-ошибок в читаемые тексты (6=DNS, 7=connect,
  28=timeout и т.д.), проверка наличия `id` в ответе.
  Бэкенд luci.warpscan логирует результат регистрации целиком в
  `/tmp/warpscan.rpc.log` (register/renewAccount/scanStart auto-register),
  а не только имя метода.
- Диагностика выяснила: регистрация через интерфейс `warp` работает
  (проверено на роутере: direct timeout → fallback через warp → http 200).
  «api unreachable even via tunnel» у пользователя возникает, когда
  интерфейс туннеля, через который идёт fallback, не существует/не поднят
  или не маршрутизирует наружу.

## 0.2.0-r55
- В ipk добавлен postinst-скрипт с
  `/etc/init.d/rpcd restart`. Без него после
  установки rpcd не перезагружается и не видит
  ucode-бэкенд — все методы luci.warpscan
  отвечают «Object not found». Раньше рестарт
  делался только вручную, поэтому у других
  пользователей пакет сразу падал.
  ucode-файл также получает chmod 644
  (rpcd молча игнорирует world-writable).

## 0.2.0-r54
- Лимит потоков поднят 24 → 32
  (scan.js max/clampInput/label,
  luci.warpscan clamp в saveSettings
  и scanStart, wscan.sh JOBS > 32 → 32).
  Для 2-ядерного роутера это потолок:
  дальше упирается сеть, не CPU.

## 0.2.0-r53
- Лимит потоков поднят 12 → 24:
  scan.js (max/clampInput/label), luci.warpscan
  (clamp в saveSettings и scanStart), wscan.sh
  (JOBS > 24 → 24). Воркеры I/O-bound,
  на 2-ядерном роутере прирост есть.

## 0.2.0-r52
- Исправлены тексты подтверждений
  перерегистрации и удаления аккаунта:
  старые ключи Cloudflare НЕ отзывает,
  они продолжают работать (прежний текст
  про «старые ключи станут недействительны»
  был неверен).

## 0.2.0-r51
- Исправлен бейдж «Зарегистрирован»:
  снова зелёный (сброс inline-стиля
  красного из r50).

## 0.2.0-r50
- Бейдж «Не зарегистрирован» — красный
  (#d9534f, белый текст).

## 0.2.0-r49
- Кнопки аккаунта переведены в горизонтальную
  строку на строке заголовка «Аккаунт WARP»
  (справа, flex-wrap:wrap); ID/Address
  сразу под заголовком, gap ~10px.

## 0.2.0-r48
- Кнопки аккаунта перенесены на строку
  заголовка «Аккаунт WARP» (сначала
  колонкой — был слишком большой зазор
  до ID).

## 0.2.0-r47
- Кнопки аккаунта
  (Зарегистрировать /
  Перерегистрировать /
  Удалить аккаунт)
  прижаты к правому
  краю блока и выровнены
  по верху (margin-left:auto, flex-shrink:0,
  убран flex-wrap — раньше
  переносились на
  новую строку вниз).
- Текст Discovery Mode: «Полезно,
  если 2408 порт заблокирован»
  (добавлено слово «порт»).

## 0.2.0-r46
- Убрана дублирующая строка
  «Аккаунт WARP зарегистрирован»
  в секции аккаунта (её
  дублирует бейдж в
  шапке). Ошибка получения
  аккаунта теперь показывается
  в accHint.
- Текст описания: «через
  ядерный AmneziaWG» → «через AmneziaWG».

## 0.2.0-r45
- Шапка страницы в стиле zeroblock:
  H2 «WARP Scanner» + описание с версией
  из метаданных opkg + строка
  статуса аккаунта WARP
  (бейдж Зарегистрирован
  / Не зарегистрирован,
  классы label success/warning темы).
- Новый rpcd-метод version: читает
  версию из /usr/lib/opkg/info/luci-app-warpscan.control.
  (учёт: флаг /m в regex не поддерживается,
  строки надо trim — CRLF в control).

## 0.2.0-r44
- Фикс: при добавлении scanStop случайно удалялась строка
  const scanned_total = length(split(hosts, '\n')) - (length(hosts) ? 0 : 1)
  в scanStatus -> ubus scanStatus падал с 'access to undeclared variable
  scanned_total' (exit 247). Из-за этого поллинг прогресса в UI
  ловил ошибку статуса, а кнопки/результаты при
  перезаходе вели себя неверно. Строка
  восстановлена, scanStatus работает.
- Диагностическая метка в консоли: WS_R44
  (проверка, что браузер не отдаёт
  кэшированный старый scan.js).

## 0.2.0-r43
- Кнопки аккаунта («Зарегистрировать/Перерегистрировать/Удалить аккаунт»)
  теперь блокируются на время скана: работающий wscan.sh уже держит старые
  ключи, а удаление/перерегистрация посреди скана дала бы битые .conf.
- Добавлена кнопка «Остановить» во время скана. Бэкенд: новый метод
  scanStop — wscan.sh пишет свой PID в /tmp/wscan/pid, scanStop берёт PGID
  из /proc/<pid>/stat, шлёт kill -TERM процесс-группе (воркеры-субшелы тоже
  гибнут), удаляет интерфейсы wgscan*/wgprobe и помечает progress=done.

## 0.2.0-r42
- Числовые поля (Хостов 1-600, Таймаут 1-10, Потоков 1-12, Пробных хостов
  1-10) теперь обрезаются к диапазону при редактировании: `max` на
  type=number только валидирует форму, но не мешал набрать 1000 и т.п.
  Добавлен clampInput() в scan.js — значение подгоняется при 'change'.

## 0.2.0-r41
- Фильтрация по странам/узлам полностью УДАЛЕНА (UI, бэкенд, wscan.sh):
  на этой сети (провайдер РФ) любой anycast-эндпоинт Cloudflare даёт выход
  только через Moscow DME = RU, так что выбор других стран всегда давал
  пустой результат и вводил в заблуждение (см. диагностику в AGENTS.md).
  Убраны: пикеры makeCheckPicker + словарь WARP_COUNTRIES в scan.js,
  параметры countries/exclude_nodes в scanStart/saveSettings (ucode),
  флаги -c/-x и логика фильтра в wscan.sh, filter_list() в бэкенде.
- Старые ключи стран/узлов из `/etc/warpscan-settings.json` очищены.

## 0.2.0-r40
- Исправлен обманчивый показ результатов: если свежий скан отфильтровал ВСЁ
  (например, выбраны страны, которых на этой сети нет), `/tmp/wscan_result.txt`
  пустой, а scanResult раньше «падал» на сохранённый файл и показывал СТАРЫЕ
  RU-результаты. Теперь существующий (даже пустой) свежий файл имеет приоритет
  — UI честно показывает «Рабочих эндпоинтов не найдено». Сохранённые
  результаты по-прежнему показываются только до первого скана.

## 0.2.0-r39
- Выпадающий список «Только страны» теперь содержит ПОЛНЫЙ список известных
  стран Cloudflare WARP (ISO-код + название, статичный словарь WARP_COUNTRIES
  в scan.js) — не только те, что нашлись в последнем скане. Рядом с каждым
  кодом показывается название («RU — Russia»), слева галочки, выбор подсвечи-
  вается. Свои коды по-прежнему можно добавлять вручную.
- Панель выпадающего списка переведена с жёстко тёмного фона на CSS-переменные
  активной темы LuCI (`--background-color-high`, `--text-color-high`,
  `--primary-color-medium` и др.) — теперь совпадает со светлой темой роутера
  (Routerich/Bootstrap), а не темнее её.

## 0.2.0-r38
- Починена читаемость выпадающих списков фильтров: панель переведена на тёмную
  тему приложения (была жёстко белая — сливалась с тёмной LuCI, отсюда «белое
  вперемешку с чёрным»). Строки вариантов теперь с фоном/рамкой, выбранные
  подсвечиваются синим, чекбоксы контрастные (accent-color).
- В панели добавлен заголовок «Нажми на пункт, чтобы выбрать / снять» и
  пояснение, если список пуст («сделайте скан, чтобы загрузить страны/узлы,
  или добавьте свой код ниже»). Кнопка ручного добавления подписана «Добавить».

## 0.2.0-r37
- Фильтры стран/узлов в UI переведены с текстовых полей на выпадающие списки
  с чекбоксами (`makeCheckPicker`): клик по кнопке открывает панель с
  вариантами, множественный выбор, своё значение можно добавить вручную,
  есть «Очистить». Выбранное считается (кнопка показывает счётчик).
- Варианты подтягиваются автоматически из последних результатов скана:
  страны (`r.country`) и узлы (`r.node`) собираются как при загрузке страницы,
  так и после каждого завершённого скана. Выбранные значения сохраняются при
  дозаполнении списка.
- Настройки по-прежнему персистентны: пикеры сохраняются через saveSettings
  и восстанавливаются из `/etc/warpscan-settings.json` (comma-строка).

## 0.2.0-r36
- Фильтр эндпоинтов при скане (UI): поля «Только страны» (напр. DE,NL,SE) и
  «Исключить узлы» (напр. DME). Пробрасываются в wscan.sh как `-c` (только
  эти `loc`) и `-x` (исключить `colo`, comma-список). Позволяет обойти
  проблему README (трафик через московский DME режется DPI в РФ).
- Бэкенд: `filter_list()` санизирует ввод до `[A-Za-z0-9, ]` (защита от shell-
  инъекций), scanStart/saveSettings принимают `countries`/`exclude_nodes`.
- Настройки фильтров персистентны (`/etc/warpscan-settings.json`).
- Проверено на роутере: без фильтров 6/6 DME RU; с `-c RU,DE -x DME,SIN`
  выборка пуста (DME исключены, других стран нет) — фильтрация работает.

## 0.2.0-r35
- Секция «Аккаунт WARP» переструктурирована: заголовок, слева колонка со
  статусом → ID и адрес → мелкое пояснение, справа колонка с кнопками
  (Зарегистрировать / Перерегистрировать / Удалить). Всё разнесено по строкам.

## 0.2.0-r34
- UI-отступы: кнопки в топ-баре результатов, в строках результата и в секции
  аккаунта разнесены через flex `gap:8px` (убраны text-ноды с одним пробелом);
  чекбокс «Включить Discovery Mode» — отступ от текста через `gap`.
- Секция «Аккаунт WARP»: вместо Peer (одинаков у всех аккаунтов Cloudflare)
  показываются id аккаунта и Address + пояснение, что меняются именно id и
  private_key, — теперь «Перерегистрировать»/«Удалить»+«Регистрировать»
  наглядно меняют id (раньше казалось, что аккаунт не изменился).

## 0.2.0-r33
- Управление аккаунтом в UI (секция «Аккаунт WARP»): кнопки «Удалить аккаунт»
  и «Перерегистрировать» с подтверждением. Перерегистрация атомарна: старый
  ключ заменяется только при успешной регистрации нового (wregister.sh пишет
  через `.tmp`+`mv`), удаление — `rm /etc/warpscan-account.json`.
- Новые методы rpcd: `deleteAccount`, `renewAccount` (+ACL на запись).
  Кнопка «Зарегистрировать WARP» показывается, только если аккаунта нет;
  конфиг-ключи в `.conf` обновляются после смены аккаунта.

## 0.2.0-r32
- Исправлено персистентное сохранение настроек в браузере: ACL-файл не содержал
  `getSettings` (read) и `saveSettings` (write), фронт молча получал отказ rpcd.
  Добавлены разрешения, переустановлено. Настройки UI (хосты/таймаут/потоки/
  discovery) сохраняются в `/etc/warpscan-settings.json` на каждый `change`.

## 0.2.0-r31
- Настройки UI сохраняются сразу при изменении (событие `change` на полях),
  а не только при старте скана.

## 0.2.0-r30
- Формат alive-лога: `(worker $w, host #$count)` вместо `($w/$count)`.

## 0.2.0-r29
- Персистентные настройки скана: `getSettings`/`saveSettings`, файл
  `/etc/warpscan-settings.json` (переживает ребут).

## 0.2.0-r28
- Логи: порядок блоков в окне «Логи» — `rpcd log` сверху, `wscan.log` снизу
  (автоскролл показывает свежий поиск). Убран спам поллинга `scanStatus`
  (каждый 3с при поллинге прогресса) — rpcd-лог теперь только значимые события:
  загрузка страницы, scanStart (с параметрами), scanResult, applyBest.

## 0.2.0-r27
- rpcd-лог очищен от пустых строк `scanStatus` (UI поллил каждые 3с → мусор).

## 0.2.0-r26
- Лимит параллельных потоков поднят с 6 до 12 (wscan.sh `-j`, бэкенд `jobs`,
  UI «Потоков (1-12)»). Бенчмарк 40 хостов: -j3=63с, -j6=34с, -j12=24с —
  AX3000 (2×Cortex-A53 + аппаратный AES/SHA) тянет 12 воркеров без деградации.

## 0.2.0-r25
- UI: у Discovery Mode убран белый фон (ломалась тёмная тема LuCI) — нейтральная
  рамка, видна на светлой и тёмной теме.

## 0.2.0-r24
- UI: Discovery Mode возвращён под кнопки поиска (уезжал в конец страницы);
  всем секциям добавлены видимые рамки/отступы.

## 0.2.0-r20…r23
- UI: настройка discovery — поле «Пробных хостов (1-10)» (флаг wscan.sh `-P N`,
  параметр scanStart `discover_hosts`). Discovery вынесен в отдельную секцию
  «Discovery Mode» с пояснением.

## 0.2.0-r19
- UI: чекбокс «Discovery Mode» перенесён вправо от кнопок поиска; убран LuCI-класс
  `cbi-input-checkbox` (скрывал нативный чекбокс) — теперь видимая галочка.
- Потери в результатах показываются только если `tun_loss > 0` (0% не выводится),
  с жёлтой меткой «Потери N%»; иначе строка чистая.

## 0.2.0-r17
- Умный port-discovery стал опцией (по умолчанию ВЫКЛ): флаг wscan.sh `-D`,
  параметр scanStart `discover:1`, чекбокс «Discovery Mode» в UI. Без опции
  скан ведёт себя как раньше (фиксированный список портов).

## 0.2.0-r14…r16
- Сделан умный port-discovery в wscan.sh `discover_ports()`: fast-path (2408 на
  2 хостах, ~1с) → если 2408 режут, fallback по 1701/4500/500 (n=2) и полному
  списку 53 портов (n=1), набор сужается до рабочих. Стоимость discovery ~1с
  на незаблокированной сети (проверено «network passes UDP», порты не меняются).

## 0.2.0-r10
- ОТКАТ: расширенный список портов WARP (из гайда) убран, оставлены
  `2408 1701 4500 500`. Причина: на сети, где 2408 режется, каждый хост
  перебирал до 53 портов × 3с и скан деградировал (10 хостов за 222с, 0 живых
  вместо 14). После отката скорость вернулась (~20с на 10 хостов).

## 0.2.0-r9
- Попытка расширить список портов WARP по гайду (53 порта). Результат хуже —
  отменено в r10.

## 0.2.0-r8
- Детект «torn down» по образцу WARPSCOUT: в фазе 2 каждый эндпоинт прогоняет
  серию ICMP через туннель (`ping -c5` до 1.1.1.1 через интерфейс wgscan).
  Если хвостовая серия из >= TEAR_RUN(2) эхо пропала (последний seq < лимита) —
  туннель резан DPI на середине потока, эндпоинт помечается `torn`.
- Новые колонки: TUN PING (rtt через туннель) и LOSS (% потерь в той же бурсте).
- Сортировка: workable (не-torn) идут вверх по пингу, torn — в конец; BEST никогда
  не выбирается из torn (фронт + applyBest).
- В UI torn-строки подсвечиваются красным и бейджиком TORN; «Скопировать лучший
  .conf» отключается, если рабочих нет. Формат строки результата стал
  `ip:port NODE LOC rtt tun_rtt tun_loss torn`.
- busybox-фикс: парсинг `seq=` (не `icmp_seq=`), `-i` не используется (интервал 1s).

## 0.2.0-r7
- фикс парсинга TUN PING.

## 0.2.0-r5
- Результаты скана сохраняются персистентно: при старте нового скана старая
  таблица копируется в `/etc/warpscan-last-result.txt` (переживает ребут роутера).
- `scanResult` при отсутствии свежего файла отдаёт сохранённый с флагом
  `saved:true`; при загрузке вкладки предыдущие результаты сразу показываются,
  заголовок «Сохранённый скан от …» / «Скан от …».

## 0.2.0-r4
- Кнопки «Быстрый поиск» и «Полный поиск (долго!)» блокируются на время
  сканирования и разблокируются по завершении или ошибке (повторный запуск
  во время идущего скана стал невозможен на фронте).

## 0.2.0-r3
- Поллинг статуса догоняет при возврате на вкладку: слушатели `focus` /
  `visibilitychange` сбрасывают таймер и сразу перечитывают статус, пока скан
  идёт (в фоновой вкладке setInterval тормозит).

## 0.2.0-r2
- Дату/время скана в шапке результатов: `scanResult` возвращает `updated`
  (mtime файла результата через `stat()`), фронт показывает «Скан от …».

## 0.2.0-r1
- Переход на схему `x.y.z-rN` в `build.sh` + `version.txt`.
- Актуальная линия версий 0.2.0 (сохранены установочные бэкапы).

## 0.1.x … 0.2.0 (ранняя линия)
- Двухфазный скан: handshake-sweep (перебор хостов) + честный ICMP-пинг с
  замером метаданных (порты 2408/1700/4500/500).
- Параллельный скан воркерами wgscan0..N-1 (`-j`, 1..6) — ускорение ~2.3х
  на 40 хостах.
- Монотонный прогресс: фаза 1 → до 80%, фаза 2 с живым счётчиком `scanned2.cnt`
  → 80..99%, done → 100%; память `/tmp/wscan/percent`, тексты фаз.
- Чистка накопления файлов воркеров (hosts.*.txt / alive.*.txt) при старте.
- `renderTable` + `confPreBox`: «Показать .conf» раскрывается/сворачивается в
  своей строке (в т.ч. последней), ранее вставлялся в конец блока.
- Копирование отдельных конфигов: «Скопировать .conf» у каждой строки +
  «Скопировать лучший .conf» (navigator.clipboard с фолбэком на
  выделение+execCommand).
- «Скачать всё .txt» → `warpscan-configs.txt`: полные конфиги для каждого
  эндпоинта с заголовком (`# ip:port node country ping ms`) и датой скана.
- Показ фаз («Фаза 1: перебор N/M, активно K», «Фаза 2: замеры, обработано N/M»).
- Кнопки переименованы: «Быстрый поиск», «Полный поиск (долго!)».

## 0.1.0 (первый рабочий)
- Базовое LuCI-приложение: форма сканирования, прогресс-бар, таблица результатов
- Регистрация WARP-аккаунта через `wregister.sh`, импорт лучшего эндпоинта.
- Обфускация (Jc/Jmin/Jmax/H/Fϕ/I) только в выдаваемых `.conf` (на сканирующем
  интерфейсе убрана — kmod-amneziawg виснул при обфускации).